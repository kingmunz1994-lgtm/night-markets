# Environment / secrets — NEVER commit
.env
.env.local

# Private state LevelDB
.private-state/

# Wallet sync state cache (speeds up re-runs; delete to force full rescan)
.wallet-state/

# Compiled contract artifacts (regenerate with: npm run compile)
contracts/managed/

# Node
node_modules/

# TypeScript
dist/
*.js.map

# Tool artifacts
.repomix-output.xml

# Nested repo clones (ecosystem repos cloned locally, not part of night-markets)
night-biz/
night-fun/
night-id/
night-lend/
night-store/
night-work/
